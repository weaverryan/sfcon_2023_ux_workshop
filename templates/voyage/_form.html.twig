{{ form_start(form) }}
    {{ form_widget(form) }}

    {% component 'Button' %}
        {% block content %}{{ button_label|default('Save') }}{% endblock %}
    {% endcomponent %}
{{ form_end(form) }}
